// ProvenanceView — which papers / repo notebooks / datasets a design drew on, from search_knowledge
// citations (docs/06). Attributes each design decision to its sources. STATUS: scaffold.
export function ProvenanceView() {
  return <div data-testid="provenance-view">ProvenanceView (scaffold)</div>;
}
