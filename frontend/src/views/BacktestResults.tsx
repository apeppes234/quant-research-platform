// BacktestResults — equity curve, drawdown, trades, metrics from QC read_backtest* (docs/04, docs/09).
// Charting lib TBD at Phase 2 (lightweight-charts | recharts — docs/14 O3). Equity + drawdown are the
// two hero charts. Rebuild from QuantDinger's panel DESIGN (its Vue source isn't available). STATUS: scaffold.
export function BacktestResults() {
  return <div data-testid="backtest-results">BacktestResults (scaffold)</div>;
}
