// IterationPanel — the define_outcome loop made visible (docs/07). The 5 rubric criteria flip ✓/✗ on
// rubric.end; iteration counter ticks. Shows the grader's per-criterion `explanation`. STATUS: scaffold.
export function IterationPanel() {
  return <div data-testid="iteration-panel">IterationPanel (scaffold)</div>;
}
