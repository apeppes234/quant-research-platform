// AgentInspector — click a node -> that thread's stream (summarized thinking, tool calls, outputs).
// Backed by the per-thread stream (threads.events.stream, docs/02). STATUS: scaffold.
export function AgentInspector() {
  return <div data-testid="agent-inspector">AgentInspector (scaffold)</div>;
}
