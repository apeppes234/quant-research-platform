// BiasLedger — the data-snooping ledger (docs/08): variants tried, in-sample vs holdout Sharpe gap,
// running trial count N, and the current Deflated Sharpe Ratio. The auditable anti-overfit trail.
// STATUS: scaffold.
export function BiasLedger() {
  return <div data-testid="bias-ledger">BiasLedger (scaffold)</div>;
}
