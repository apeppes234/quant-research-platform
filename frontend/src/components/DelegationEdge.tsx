// DelegationEdge — custom React Flow edge that animates on delegation (Manager->specialist) and on
// results (specialist->Manager). dir="delegate" vs "result" (from agent.thread_message_sent/_received).
// Optionally renders an artifact chip (features.parquet / algo.py / results.json) traveling the edge.
// STATUS: scaffold.
// import { EdgeProps, getBezierPath } from "reactflow";
export function DelegationEdge(/* props: EdgeProps */) {
  return null; // scaffold
}
