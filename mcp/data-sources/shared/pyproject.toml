[project]
name = "mcp-data-sources"
version = "0.0.1"
description = "Thin PIT-safe data-source MCP wrappers for FRED/ALFRED, EDGAR, GDELT, and arXiv."
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.9.3",
    "httpx>=0.27",
]
